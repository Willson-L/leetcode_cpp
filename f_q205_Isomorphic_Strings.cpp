#include <stdio.h>
#include <iostream>
#include <string>
#include <vector>
using namespace std;
class Solution {
public:
    bool isIsomorphic(string s, string t) {
        vector<int> s_num;
        vector<int> t_num;

        for(int i =0; i< num.size(); i++){
            cout << num[i];
        }
        for(int i =0; i< s.size(); i++){
            for(int j =i+1; j< s.size(); j++){
                if(s[i]==s[j]){
                    
                }
            }
        }
        return true;
    }
};
int main(int argc, char * argv[]){
    string s="bb";
    string t="cc";
    bool result;
    class Solution cSolution;    
    result = cSolution.isIsomorphic(s, t);    
    cout << result;
    system("pause");
}
